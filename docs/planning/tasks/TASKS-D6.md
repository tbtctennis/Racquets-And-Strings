# D6 task breakdown — corrections and the partner pool

> **41 tasks across 26 jobs**, sliced by the method in [tasks/README.md](README.md): each task
> cuts a complete path through the layers it touches and is verifiable on its own. Wide
> refactors (C20, C21) are sequenced expand, migrate, contract instead.

> **C22 has left this sprint.** The year end halving of league points cannot run until the
> wallet split lands, and the owner deferred that split to a future milestone. See
> [Deferred: C22](#deferred-c22-league-points-at-the-year-boundary) at the foot of this file.

|              |                                                                  |
| ------------ | ---------------------------------------------------------------- |
| **Sprint**   | [D6 — corrections and the partner pool](../sprints/d6-d9/SPRINT-D6.md) |
| **Phase**    | [M1 — the five non-negotiables](../VISION.md)                    |
| **Register** | [TASK-REGISTER.md](TASK-REGISTER.md)                             |

## The standard exit gate

**Every task below closes on the same four conditions**, so they are not repeated per row:

1. Its acceptance criteria all pass
2. `npm run verify` is green
3. Claude review PASS **and** OpenAI review PASS
4. No findings outstanding

The **Exit adds** column lists only what a task requires _beyond_ that. An empty cell means the
standard gate is the whole gate.

> **D6 V2 is a policy, not a task.** "A test for every correction" is enforced through each
> task's acceptance criteria, every one of which must fail on today's code.

---

## Verify first

| ID           | Task                      | Before                                                                                                 | After                                                                           | Acceptance                                                                                      | Exit adds                         |
| ------------ | ------------------------- | ------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | --------------------------------- |
| **D6-V1-T1** | Confirm the base is green | Three commits (`411ffed`, `5cdacee`, `54cea44`) show a failed check and nothing records it being fixed | The branch is known green at one exact commit, or every failure is written down | `npm run verify` runs to completion on the base commit; each failure is linked to a backlog row | **Blocks every other task in D6** |

---

## Corrections

| ID            | Task                                   | Before                                                                                                                                                                | After                                                                                                                                                                                                  | Acceptance                                                                                                                                                                                                                                                                                                                   | Exit adds                                                                                                                                                                   |
| ------------- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D6-C1-T1**  | Unblock the knockout gate              | The `every` demands all group matches complete, and the placeholder guard is ANDed into the predicate, so a one-player group shuts the gate forever                   | Real matches are filtered, not ANDed; unplayed matches warn instead of blocking                                                                                                                        | A one-player group can open the knockout · a draw with unplayed matches opens and shows the count · no real group matches still refuses                                                                                                                                                                                      | —                                                                                                                                                                           |
| **D6-C2-T1**  | Restore points won and total played    | The writer was removed but every reader is live, so each member's P/G % is frozen and new members read a dash forever                                                 | Both figures move on the played branch for winner and loser; the walkover branch is untouched                                                                                                          | A scored match moves both figures for both players · a rescore equals a fresh recompute · a walkover moves neither                                                                                                                                                                                                           | —                                                                                                                                                                           |
| **D6-C3-T1**  | `tournamentsPlayed` on join            | The loser branch adds 1 on **every loss**; the winner only on the final                                                                                               | It increments once in `onParticipantCreated` when the event is a tournament, and never on a result                                                                                                     | Joining a tournament increments it once · scoring a match never moves it · withdrawal leaves it alone                                                                                                                                                                                                                        | —                                                                                                                                                                           |
| **D6-C3-T2**  | `tournamentsPlayed` backfill           | Stored values equal each member's loss count                                                                                                                          | Values equal the number of distinct tournament events the member has a participant row for                                                                                                             | Dry run against the seeded emulator produces a diff; applying it makes every value match the recount                                                                                                                                                                                                                         | Owner reads and approves the diff                                                                                                                                           |
| **D6-C4-T1**  | Delete `loses` from code               | Written by three functions, carried through three type definitions, rendered on no screen                                                                             | The field is gone from writers, types, normalisation, the leaderboard row and the seed                                                                                                                 | `grep -rn "\bloses\b" src/ functions/` returns nothing outside prose · typecheck clean · no screen changes                                                                                                                                                                                                                   | —                                                                                                                                                                           |
| **D6-C4-T2**  | Strip `loses` from stored data         | Every `stats` document still carries the dead field                                                                                                                   | The field no longer exists in stored data                                                                                                                                                              | Dry run lists the affected documents; after the migration none carries `loses`                                                                                                                                                                                                                                               | Owner reads and approves the diff                                                                                                                                           |
| **D6-C5-T1**  | One award table                        | A third private copy of the points table lives in `withdrawalWorkflow.js`, matching the shared one only by coincidence                                                | The withdrawal path imports the shared `tournamentAward`; the private copy is deleted                                                                                                                  | Withdrawal payouts are unchanged · a withdrawing player's `matchesPlayed` and `wins` stay put, pinned by a test · `grep -c "R32: 1" functions/` returns 1                                                                                                                                                                    | —                                                                                                                                                                           |
| **D6-C6-T1**  | Rename the booking stamp               | `completion_requested_at` says something was requested; the stringer is stating the job is done                                                                       | The field is `marked_completed_at` across functions, types and tests                                                                                                                                   | The rename lands in all five files · booking state tests pass · no migration needed (the one test booking is cancelled)                                                                                                                                                                                                      | —                                                                                                                                                                           |
| **D6-C7-T1**  | Delete dead code                       | `useJoin.ts` keeps unreachable `full` / `fallback` branches — one of which rewrote a member's skill level — and `selectGroupWinners` has no callers                   | Both are gone, with their plumbing                                                                                                                                                                     | `grep -rnE "slotStatus\|selectGroupWinners" src/` returns nothing · typecheck clean                                                                                                                                                                                                                                          | —                                                                                                                                                                           |
| **D6-C8-T1**  | Remove group lessons and lock          | `group_lessons` and `redemption_locks` survive server-side after the UI was removed                                                                                   | Both blocks, their functions and their tests are deleted                                                                                                                                               | `grep -rn "group_lessons\|redemption_locks" firestore.rules functions/` returns nothing · rules tests still pass                                                                                                                                                                                                             | —                                                                                                                                                                           |
| **D6-C8-T2**  | Rebuild coach ↔ player contacts        | `isCurrentGroupLessonCoachFor` is deleted with the collection, silently removing a contact path the owner reinstated                                                  | The mutual coach ↔ player rule is rebuilt against the **coaching session**, not the retired collection                                                                                                 | A coach reads their session player's contacts and the player reads theirs · an unrelated member is denied · rules tests cover both                                                                                                                                                                                           | —                                                                                                                                                                           |
| **D6-C8-T3**  | Book and Redeem Discount create a lead | `bookService` exists but no Book control was found on any service card, so the booking flow has no entry point, and nothing adds a member to a provider list of leads | Every service card carries **Book** and **Redeem Discount**. Either one adds the member to that provider list of leads and creates the member to provider connection                                   | Both controls appear on every service card · using either adds the member to the provider leads · using either creates the connection, so contacts become visible both ways · the controls use the shared element set                                                                                                        | Needs **D6-C12-T1** for rules                                                                                                                                               |
| **D6-C9-T1**  | No-show leftovers                      | Rules still whitelist `no_show`, so a client can set it, and the metrics compute still branches on it                                                                 | The whitelist entry and the branch are gone; the metric is walkover or played                                                                                                                          | `grep -rn "no_show" firestore.rules functions/` returns nothing · a client write of the field is denied                                                                                                                                                                                                                      | —                                                                                                                                                                           |
| **D6-C10-T1** | Retire `result_application`            | The idempotency hash lives in two places and the duplicate check requires both                                                                                        | The applied submission is marked inside `result_submissions` and the duplicate check reads only that                                                                                                   | A duplicate submission is still refused · advancement still detects an occupied target                                                                                                                                                                                                                                       | **Lowest priority — defer if the sprint runs long**                                                                                                                         |
| **D6-C11-T1** | Mistyped addresses go home             | A catch-all route renders a Not Found page                                                                                                                            | Any unknown path redirects to home with `replace`, and the page is deleted                                                                                                                             | `/nonsense` lands on home · Back does not re-enter the redirect · `NotFound.tsx` is gone                                                                                                                                                                                                                                     | —                                                                                                                                                                           |
| **D6-C12-T1** | `services` security rules              | `firestore.rules` has no `services` block at all, so the catalogue falls through to deny                                                                              | The catalogue reads publicly and every client write is denied                                                                                                                                          | An anonymous read succeeds · an authenticated write is denied · a super-admin write is denied                                                                                                                                                                                                                                | **Unblocks D6-C8-T3**                                                                                                                                                       |
| **D6-C13-T1** | Score margin threshold 10 → 21         | Scores above 10 require a margin of 2, so a legitimate `12-2` is rejected                                                                                             | The threshold is 21 in all three layers, changed in one commit                                                                                                                                         | `21-19` valid · `22-19` rejected · `12-2` accepted — asserted in rules, functions and client                                                                                                                                                                                                                                 | All three layers in one commit                                                                                                                                              |
| **D6-C14-T1** | No re-notification on a lower score    | A same-winner reconcile re-applies and notifies both players again, though the outcome never changed                                                                  | A same-winner reconcile applies silently                                                                                                                                                               | A lower-margin resubmission updates the score and sends nothing · a winner change still notifies · a dispute still notifies once                                                                                                                                                                                             | —                                                                                                                                                                           |
| **D6-C15-T1** | One result model — server              | Challenges and rallies each run their own five-state handshake, neither matching how a tournament result is entered                                                   | Both adopt the tournament result model: either player submits, applies immediately, lower margin wins, conflicting winners raise a dispute                                                             | Either player can submit on a challenge and a rally · no confirmation step remains · a conflicting winner flags a dispute · an unsubmitted rally pays nothing                                                                                                                                                                | Do with **D6-C21** — same code path                                                                                                                                         |
| **D6-C15-T2** | One result model, client and words     | The UI carries accept, report, confirm, and `rejected` duplicates `declined`                                                                                          | The handshake UI is gone and declining notifies the creator                                                                                                                                            | `grep -rn "'rejected'" src/ functions/` returns nothing · declining a challenge notifies its creator · `ladder_cancelled` still means the challenger withdrew their own open challenge                                                                                                                                       | Needs **D6-C15-T1**. `ladder_cancelled` is a **notification type** (`useNotifications.ts:32`, `notifications.js:362`), not a stored column, so nothing schema level changes |
| **D6-C16-T1** | Re-seat a re-added participant         | The placer is create-only and looks for an open slot, so a re-added player is never placed by either mechanism                                                        | An update trigger fires on `withdrawn` to `active`. Placement then follows the event type: a **Round Robin** tournament gives them group matches, a **knockout** tournament puts them back in the draw | Re-added **before** matches are generated: they take an open slot in either format · re-added **after** generation: they go to the unplaced players list and the organizer assigns the slot · an existing bracket is never rebuilt and nobody already playing is moved · a test covers the re-add round trip in both formats | —                                                                                                                                                                           |
| **D6-C17-T1** | Four event types                       | The modal permits values outside the four                                                                                                                             | Exactly four types are offered and anything else is refused on write                                                                                                                                   | The modal offers four · a write outside them is rejected                                                                                                                                                                                                                                                                     | —                                                                                                                                                                           |
| **D6-C17-T2** | Event type casing migration            | Live data holds five values across ten events, including `tournament` beside `Tournament`                                                                             | Every event carries one of the four exact values                                                                                                                                                       | Dry run finds the lower-case row; after migration no event is outside the four                                                                                                                                                                                                                                               | Owner approves the diff · closes BLG0019                                                                                                                                    |
| **D6-C18-T1** | Remove per-event draw hiding           | `hide_seniors` and `hide_beginners` let an organizer hide draw tabs that participation already governs                                                                | Both fields and their filters are gone; the Retired Pro and Beginners draws stay                                                                                                                       | Both draws always render · `grep -rn "hide_seniors\|hide_beginners" src/` returns nothing · a migration strips the fields                                                                                                                                                                                                    | —                                                                                                                                                                           |
| **D6-C19-T1** | Change Your Zone                       | The tournament page runs a second "Request Zone Change" path needing organizer approval                                                                               | A member changes zones directly and the organizer is notified. The control reads **Change Your Zone**                                                                                                  | The button reads "Change Your Zone", not "Request Zone Change" · changing zone needs no approval anywhere · the organizer receives a notification · no approval UI remains                                                                                                                                                   | Folds into **D6-F2**                                                                                                                                                        |
| **D6-C23-T1** | Beta notifications stay in-app         | Server-side email paths can fire during the beta                                                                                                                      | Nothing leaves the app; the in-app record is the only delivery                                                                                                                                         | No beta-triggered path sends email · in-app notifications still record and display · a test asserts the outbound path is not called                                                                                                                                                                                          | —                                                                                                                                                                           |

---

## C20 — the `location` field · wide refactor

Sequenced expand → migrate → contract, so the suite stays green between batches.

| ID            | Task                          | Before                                                                        | After                                                                                                                                                                                                 | Acceptance                                                                                                                                                                                                                                                                                                                       | Exit adds       |
| ------------- | ----------------------------- | ----------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| **D6-C20-T1** | Expand, add `location`        | No city concept exists; `league` means Men's / Women's and must not be reused | `location` exists beside what is there and is derived from the member preferred courts. **`Toronto` is written only when those courts include a Toronto court**, never as a blanket default           | A member whose courts include a Toronto court gets `Toronto` · a member with no courts, or courts that map to no city, is left **unset** · no member is assigned a location they did not earn through their courts · nothing else changes behaviour                                                                              | Blocks T2 to T4 |
| **D6-C20-T2** | Migrate — rules and functions | No server path knows about locations                                          | Rules and callables read `location` while the old behaviour still works                                                                                                                               | Rules and functions read the field · every existing test still passes                                                                                                                                                                                                                                                            | Needs T1        |
| **D6-C20-T3** | Migrate — client read paths   | The client has no location awareness                                          | Client reads use `location`, in batches that each keep the suite green                                                                                                                                | Each batch leaves `npm run verify` green · no UI change is visible yet                                                                                                                                                                                                                                                           | Needs T1        |
| **D6-C20-T4** | Contract, enforce scoping     | Any member can challenge any other                                            | A cross-location challenge or rally is refused server-side. Cross-zone inside a location still works, and a member with an **unset** location plays within the location of any event they have joined | A cross-location challenge and rally are both refused by rules **and** by the callable · a cross-zone challenge inside Toronto succeeds · **a member with no location set can still send and receive challenges and rallies, scoped to the location of any event they have joined** · the leaderboard still shows every location | Needs T2 and T3 |

## C21 — `friendly` → `rally` · wide refactor

| ID            | Task                          | Before                                                                             | After                                                         | Acceptance                                                                                  | Exit adds                               |
| ------------- | ----------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | --------------------------------------- |
| **D6-C21-T1** | Expand — add the rally form   | `friendly` is load-bearing in the service, its callers, tests and stored documents | The `rally` form exists beside `friendly`; both work          | Both names resolve · every existing test passes                                             | Blocks T2 and T3 · pair with **D6-C15** |
| **D6-C21-T2** | Migrate — call sites and data | Every caller and every stored document uses the old term                           | Callers and stored documents use `rally`, migrated in batches | Each batch leaves the suite green · no stored document carries the old term                 | Needs T1                                |
| **D6-C21-T3** | Contract — delete `friendly`  | The old form still exists                                                          | Only `rally` remains                                          | `grep -rni "friendl" src/ functions/ tests/` returns nothing outside archived planning docs | Needs T2                                |

---

## Features

| ID           | Task                            | Before                                                                                               | After                                                                                   | Acceptance                                                                                                                                                                  | Exit adds             |
| ------------ | ------------------------------- | ---------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- |
| **D6-F1-T1** | Partner pool — schema and rules | The join sheet promises a pool that does not exist; a member leaving the field blank joins nothing   | Two subcollections under the event, with membership checkable in one lookup             | Any signed-in member reads the member list · **only pool members** read pool contacts · a non-member is denied                                                              | Blocks T2–T4          |
| **D6-F1-T2** | Partner pool — server           | No join, leave or pairing logic exists                                                               | `joinPool`, `leavePool` and pairing run server-side                                     | Joining and leaving write the expected documents · a member cannot join twice · the seven pool tests pass                                                                   | Needs T1              |
| **D6-F1-T3** | Partner pool — client hooks     | No client access to the pool                                                                         | `usePool` and `usePoolContacts` expose it                                               | The hooks read the pool and its contacts within the rules · a non-member sees no contacts                                                                                   | Needs T1              |
| **D6-F1-T4** | Partner pool — panel            | The join sheet's promise is untrue                                                                   | A panel lists the pool and lets a member join or leave; the join-sheet hint is now true | The panel renders the pool using the shared `PersonRow` · joining from the sheet lands the member in the pool                                                               | Needs T2 and T3       |
| **D6-F2-T1** | Courts, not zones, at join      | A member with no courts chosen is silently assigned to Downtown-Midtown, and zone is picked directly | A member picks courts and the zone is derived; an unmapped court lands them in Unplaced | The five zone conditions hold · an unmapped court lands in Unplaced · no member is silently assigned                                                                        | Carries **D6-C19-T1** |
| **D6-F2-T2** | Join surface for courts         | The join UI asks for a zone                                                                          | The join UI asks for courts and shows the derived zone                                  | The surface asks for courts · the derived zone is shown · it uses the shared element set                                                                                    | Needs T1              |
| **D6-F3-T1** | Knockout size moves both ways   | The size bar only expands                                                                            | Size moves both ways in edit mode, with 4 as the floor and played matches protected     | 8→16 keeps every match · 16→8 with an empty upper half succeeds · 16→8 with a played match is refused, naming it · the bar is inert outside edit mode · 4 cannot be reduced | —                     |

---

## Deferred: C22, league points at the year boundary

**Not in D6, and not in the beta.** Recorded here so the reasoning is not rediscovered.

The [2026-08-31 ruling](../specs/2026-08-31-vision-gaps-design.md) says a season's **league
points** halve at the end of the year, and that the halving applies to **ranking only**, leaving
the member's spendable balance whole.

The app cannot do that today. `functions/rewards.js:4` computes a member's redeemable balance as
`leaguePoints26` plus earned task points minus points already spent, so league points are also
half the wallet. Halving them now would halve unspent purchasing power, which is the one outcome
the ruling forbids.

The same ruling defers the **wallet split** to a future milestone and sets one hard constraint:
**the split must land before the first halving ever runs.** So both pieces are end of year work,
in this order.

| ID         | Task                              | Blocked by                     |
| ---------- | --------------------------------- | ------------------------------ |
| _(future)_ | Split the wallet from the ranking | Backlog, end of year milestone |
| **C22**    | Halve league points at year end   | The wallet split               |

Two properties the halving needs whenever it is built: it must be **idempotent**, since running
it twice must not halve twice, and the rollover is a **code change**, because `leaguePoints26` is
a literal field name in the rules file, seven Cloud Functions, four test files and D8's seeding
query.
