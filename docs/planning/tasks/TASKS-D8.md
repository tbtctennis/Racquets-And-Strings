# D8 task breakdown — seeding, the coaching pool, and the workflow record

> **12 tasks across 7 jobs.** The seeding engine is built as pure functions first, so every
> rule has a unit test with no emulator, and only then wired into the draws.

|              |                                                                                     |
| ------------ | ----------------------------------------------------------------------------------- |
| **Sprint**   | [D8 — seeding, the coaching pool, and the workflow record](../sprints/d6-d9/SPRINT-D8.md) |
| **Phase**    | [M3 — additional scope](../VISION.md)                                               |
| **Register** | [TASK-REGISTER.md](TASK-REGISTER.md)                                                |

## The standard exit gate

Every task closes on: acceptance criteria pass · `npm run verify` green · Claude review PASS
and OpenAI review PASS · no findings outstanding. **Exit adds** lists only the extras.

## What blocks this sprint

D8 cannot start clean until D6 and D7 land. The dependencies are real, not sequencing
preference:

| Needs          | Because                                                                                         |
| -------------- | ----------------------------------------------------------------------------------------------- |
| **D6-C2-T1**   | The seeding tiebreak reads P/G won %. Without C2 it is frozen and every tiebreak resolves wrong |
| **D6-C7-T1**   | C7 deletes `selectGroupWinners`; S1 builds `seeding.ts` fresh rather than reviving the stub     |
| **D6-C1-T1**   | The knockout gate is pinned shut, so there is no knockout to seed                               |
| **D7-CS3b-T1** | No row slot exists, so a seed number has nowhere to render                                      |

---

## S1 · The seeding engine

| ID           | Task                                  | Before                                                                                                          | After                                                                                                                                                        | Acceptance                                                                                                                                                                                                                                                         | Exit adds                    |
| ------------ | ------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------- |
| **D8-S1-T1** | `seedCount`                           | No seeding exists; R-4 removed ordering entirely                                                                | A pure function returns half the draw capped at 10. **Knockouts only.** Round Robin group formation stays zone, then skill, then courts, and is never seeded | Returns 2, 4, 8, 10 for draws of 4, 8, 16, 32 · covered by a unit test with no emulator · group formation is untouched                                                                                                                                             | New file, pure, no Firestore |
| **D8-S1-T2** | Entry ordering                        | Nothing orders entrants; `stats.rankPosition` is a weekly snapshot that would seed on rank up to six days stale | Order is live `leaguePoints26`, then P/G won %, then name ascending — a total order                                                                          | Two players on equal points and equal P/G % order alphabetically · a third joining above both renumbers them 2 and 3 · no two players share a seed                                                                                                                 | Needs **D6-C2-T1** for P/G % |
| **D8-S1-T3** | `seedAnchors` and automatic placement | No placement construction exists, and slots are filled in entry order                                           | The recursive anchor construction places every seed automatically, so the top two seeds can only meet in the final                                           | An 8 draw pairs 1v8, 4v5, 3v6, 2v7, which puts 1 and 2 in opposite halves and sets the semifinals as 1v4 and 2v3 · the same construction generalises to 16 and 32 · byes go to the top seeds · placement needs no organizer input · every function has a unit test | —                            |

## S2 · Wire seeding into the draws

| ID           | Task                      | Before                                     | After                                                                                                                                                               | Acceptance                                                                                                                                                                                 | Exit adds                                |
| ------------ | ------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------- |
| **D8-S2-T1** | `seed` on the player type | `TournamentPlayer` has no seed             | It carries `seed?: number`                                                                                                                                          | The type compiles everywhere it is consumed                                                                                                                                                | —                                        |
| **D8-S2-T2** | RR knockout ordering      | Knockout order comes from fill order       | The knockout generated from Round Robin groups **is** a knockout, so it is seeded: group points across all groups, then P/G won %, then leaderboard rank, then name | The four criteria apply in order · scope is one draw, so seed 1 is the top player in that bracket · the groups themselves remain unseeded                                                  | Needs S1-T2                              |
| **D8-S2-T3** | Place by anchors          | `buildRRKnockoutDocs` places in fill order | It places by `seedAnchors`, byes to the top seeds                                                                                                                   | Seed 1 top and seed 2 in the opposite half · 3 and 4 anchor the other quarters · byes land on top seeds · regenerating produces the same bracket · group formation is byte-identical to D7 | `buildZoneTierGroups` is **not** touched |

## S3–S5 and the sprint's other work

| ID            | Task                                | Before                                                                                                                              | After                                                                                                                                                                                                                                                            | Acceptance                                                                                                                                                                                                                                                                                                                                                    | Exit adds                                                                                                                                                                     |
| ------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D8-S3-T1**  | Seed display                        | Seeds are computed but invisible                                                                                                    | `(1)` renders before the name in the bracket, draw list and standings                                                                                                                                                                                            | Seeded rows show the number · unseeded rows show nothing, not `(0)` · every row type still fits 360px                                                                                                                                                                                                                                                         | Needs **D7-CS3b-T1** · the badge replaces one of the two stats, never becomes a third                                                                                         |
| **D8-S4-T1**  | Seeds freeze, unseeded stay movable | Seeds recompute on every join, so a strong late entrant pushes everyone down, and nothing can be adjusted afterwards                | Seeding fixes the moment the draw is generated. After that the organizer may move **unseeded** players by hand                                                                                                                                                   | Joining an ungenerated draw reorders seeds · joining a generated draw changes no existing seed · the organizer can move an unseeded player to another open position after generation · a seeded player keeps their number and position · reseeding a generated draw stays forbidden                                                                           | —                                                                                                                                                                             |
| **D8-S5-T1**  | Coaching pool documented            | The coaching pool exists in no workflow record, and it inherits five unanswered questions from DATA_SHAPE §9                        | §15.3 documents the target. A coaching service offers three actions, **Book group lesson**, **Book**, and **Redeem discount**. Pooling is not limited to socials, and bookings and offers gain a **type**: group classes, private classes, stringing, extensible | The section is **appended**, not inserted · no section is renumbered · no cross-reference breaks · the three actions and the type list are recorded · pooling is described for any event, not only socials                                                                                                                                                    | Documents the target, does not build it. **The old `group_lessons` collection is never revived.** D6-C8-T1 deletes it; a group lesson becomes a booking of type group classes |
| **D8-S5-T2**  | Account creation documented         | The sign-up journey is undocumented                                                                                                 | A new §19 covers the email gate, password, profile completion, throttling and the duplicate-address path                                                                                                                                                         | The section is appended after the existing last one · status vocabulary rows are added at §16                                                                                                                                                                                                                                                                 | Append only                                                                                                                                                                   |
| **D8-RNK-T1** | Restore `rankPosition`              | DC-11 deleted it as "rendered nowhere", but three files read it and a function writes it                                            | The field is restored, with seeding as a second consumer                                                                                                                                                                                                         | The three readers work · the snapshot writes it · seeding can read it                                                                                                                                                                                                                                                                                         | DC-11's premise was wrong                                                                                                                                                     |
| **D8-SHP-T1** | Update the data shape               | `shape-reference.mjs` and `build-sample-dataset.mjs` still encode pre-D6 rulings, and carry no room for collections that are coming | Seven corrections applied, plus headroom for future collections, payments first                                                                                                                                                                                  | `pointswon` and `totalPointsPlayed` restored · `tournamentsPlayed` carries its new meaning · `rankPosition` restored · `completion_requested_at` becomes `marked_completed_at` · the partner pool stored, not derived · `result_application` retired · `points_winner` and `points_loser` added · the shape admits future collections, with payments modelled | `npm run dataset:build` stays green                                                                                                                                           |

---

## Not in this sprint

The lesson add-on itself is documented, not built. Provider contact through a booking
connection stays deferred. Reseeding a generated draw is ruled out.
