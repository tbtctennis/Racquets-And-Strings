# D7 task breakdown — the shared component set

> **56 tasks across 7 groups.** The per-site sweeps are batched by concern rather than by
> audit row: each batch is one mechanical change across many files, verifiable by the grep
> suite, which is the only automated check on visual work.

|              |                                                               |
| ------------ | ------------------------------------------------------------- |
| **Sprint**   | [D7 — the shared component set](../sprints/d6-d9/SPRINT-D7.md)      |
| **Phase**    | [M2 — UI/UX simplification](../VISION.md)                     |
| **Spec**     | [M2 spec](../specs/2026-08-31-m2-uiux-simplification-spec.md) |
| **Register** | [TASK-REGISTER.md](TASK-REGISTER.md)                          |

**Line numbers are `dev-anuj` @ `ac4dfb1`.** Re-check before editing.

## The standard exit gate

Every task closes on: acceptance criteria pass · `npm run verify` green · Claude review PASS
and OpenAI review PASS · no findings outstanding. **Exit adds** lists only the extras.

**Two conditions apply to every task in this sprint**, from the sprint's binding rule and its
verification table:

- The primitive is registered in `.design-sync` **in the commit that creates it**, and consumed
  at one real call site in the same change. The build compiles CSS from `src/` only.
- Checked in **light and dark**. Six of the eight original audit passes found light-theme-only
  defects, so a dark-only check proves nothing.

Rows built in groups 1 and 2 also have to pass at **360px** before they count as done.

---

## Group 1 · Person components

| ID             | Task                          | Before                                                                                                                                                        | After                                                                                                                           | Acceptance                                                                                                                                                                                         | Exit adds                            |
| -------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------ |
| **D7-CS1-T1**  | One name formatter            | Three formatters exist. `formatPersonName` trims but does **not** title-case, so six members render `blake bell` on a row and `Blake Bell` on the leaderboard | One formatter title-cases, keeps the fallback, and keeps the `PLAYER_LOADING` / `BYE` / `Winner of …` guards                    | Every surface renders `Blake Bell` · the bracket still renders `BYE` and `Winner of QF1`, not `Bye` / `Winner Of Qf1` · `toTitleCase` and `formatPlayerName` are deleted and every call site moved | **Blocks six components — do first** |
| **D7-CS3-T1**  | `PersonRow` densities         | 41 ways of drawing a person                                                                                                                                   | `PersonRow` carries three densities and an `editControls` slot that absorbs the RR standings row                                | All three densities render · the RR standings row uses the slot · every density fits 360px                                                                                                         | Needs CS1-T1                         |
| **D7-CS3b-T1** | `seed` slot                   | No slot exists for a seed number                                                                                                                              | `PersonRow` renders `(1)` before the name when a seed is present                                                                | A seeded row shows the number · an unseeded row shows nothing, not `(0)` · the badge counts against the two-stat budget                                                                            | **Unblocks D8-S3**                   |
| **D7-CS8-T1**  | `PersonOption`                | Nine picker surfaces each draw a person their own way                                                                                                         | One `PersonOption` serves every picker                                                                                          | All nine pickers use it · keyboard selection works · it fits 360px                                                                                                                                 | Needs CS1-T1                         |
| **D7-CS9-T1**  | `PersonPairRow`               | Seven copies of `{p1} vs {p2}`                                                                                                                                | One component draws every pairing                                                                                               | All seven call sites use it · names truncate rather than wrap                                                                                                                                      | Needs CS1-T1                         |
| **D7-CS10-T1** | `PersonChip` / `PersonInline` | Inline person references are ad hoc                                                                                                                           | Two small components cover chip and inline use                                                                                  | Both exist, are registered in `.design-sync`, and are consumed at a real call site                                                                                                                 | Needs CS1-T1                         |
| **D7-CS17-T1** | `initialOf` and avatars       | Three first-initial implementations. Navbar avatars are 20px and provider 24px, and player rows carry no avatar at all                                        | One `initialOf`. **Every player row shows a 24px avatar before the name**, and the profile card uses 96px. No other size exists | One implementation remains · every player row renders a 24px avatar ahead of the name · the profile card renders 96px · no third size appears · rows still fit 360px with the avatar in place      | Needs CS1-T1                         |
| **D7-CS20-T1** | The 78px action slot          | A fixed action slot exists but half the call sites defeat it with `w-auto`                                                                                    | The fixed 78px slot holds everywhere                                                                                            | No call site overrides the width · the name never shrinks below ~40% of the row                                                                                                                    | —                                    |
| **D7-CS21-T1** | Expanded-row behaviour        | Expanded rows behave differently per surface and the member's own row highlights twice                                                                        | One expanded-row behaviour; one highlight                                                                                       | Expansion behaves identically across surfaces · the own-row highlight appears once                                                                                                                 | —                                    |
| **D7-AX25-T1** | Names for screen readers      | Confirm text and screen-reader labels use raw stored names                                                                                                    | Both use the formatted name                                                                                                     | Screen-reader labels and confirm text read `Blake Bell`                                                                                                                                            | Needs CS1-T1                         |

---

## Group 2 · Cards, tiles and rows

| ID             | Task                    | Before                                                                                                                                       | After                                                     | Acceptance                                                                                                                                 | Exit adds                                                                                   |
| -------------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| **D7-CS2-T1**  | `StatGrid`              | Seven tile geometries                                                                                                                        | One grid around the existing `StatTile`                   | Every stat cluster uses it · tiles align on one geometry                                                                                   | —                                                                                           |
| **D7-CS4-T1**  | `ListRow` + `ListGroup` | Eight copies of one skeleton                                                                                                                 | Two components serve all eight                            | All eight surfaces use them · rows fit 360px                                                                                               | —                                                                                           |
| **D7-CS5-T1**  | `EntityCard`            | Five copies of one footer card                                                                                                               | One card                                                  | All five use it                                                                                                                            | —                                                                                           |
| **D7-CS6-T1**  | `ReviewPanel`           | Five organizer queues in five different chromes                                                                                              | One panel                                                 | All five queues use it                                                                                                                     | —                                                                                           |
| **D7-CS7-T1**  | One `ProfileCard`       | **Three** components, 2,021 lines — `Profile.tsx` 651, `ProfileInfo.tsx` 983, `PlayerProfile.tsx` 387 — with duplications on different pairs | One card with `mode: 'own' \| 'public'`                   | Both surfaces render from one card · the streak, P/G % and `initialOf` duplications are gone · `Phone` / `Contact` label drift is resolved | **Do before the leaderboard row.** The safety boundary is the rules file, not the component |
| **D7-CS11-T1** | `ApprovePair`           | Six hand-written approve/reject pairs                                                                                                        | One component                                             | All six use it                                                                                                                             | —                                                                                           |
| **D7-CS13-T1** | Queue heading pattern   | Queue headings vary                                                                                                                          | Every queue reads `{Title} ({n})`                         | Every queue heading matches the pattern                                                                                                    | —                                                                                           |
| **D7-CS14-T1** | Fold disclosures        | Four hand-rolled disclosures beside the existing `Accordion`                                                                                 | All four use `Accordion`                                  | No hand-rolled disclosure remains                                                                                                          | —                                                                                           |
| **D7-CS15-T1** | One drawer layout       | Expanded drawers differ per surface                                                                                                          | One layout                                                | Every drawer uses it                                                                                                                       | —                                                                                           |
| **D7-CS16-T1** | `PlaceCard`             | The court-map popup is its own surface                                                                                                       | It becomes a **density** of `PlaceCard`, not a rewrite    | The popup renders as a density · the map still behaves                                                                                     | —                                                                                           |
| **D7-CS43-T1** | Bracket column width    | The desktop bracket is squeezed inside `max-w-xl`                                                                                            | The column is widened or moved out, with a `slot` variant | The bracket reads on desktop · mobile is unchanged                                                                                         | —                                                                                           |

---

## Group 3 · Overlays, forms and states

| ID             | Task                    | Before                                                                                               | After                                                   | Acceptance                                                                                | Exit adds                                                                     |
| -------------- | ----------------------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ----------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------- |
| **D7-MF8-T1**  | `FieldError` everywhere | 15 inline error paragraphs                                                                           | All 15 use the existing `FieldError`                    | No inline error paragraph remains                                                         | —                                                                             |
| **D7-MF9-T1**  | One banner              | 13 hand-rolled banners against 2 real consumers; 11 have no box and none announce to a screen reader | `AlertMessage` is the only banner, with `role="alert"`  | Hand-rolled banners 13 → 0 · every banner announces                                       | —                                                                             |
| **D7-MF10-T1** | `ConfirmSheet`          | **Five** browser `confirm()` calls, unstyleable and device-dependent                                 | Every one is a modal form with yes and no               | Browser `confirm()` 5 → 0 · each modal carries its own heading · keyboard and Escape work | **Sequence after the withdrawal work** — `Tournament.tsx:535` is that confirm |
| **D7-MF11-T1** | `Popover`               | Three popovers sit in flow and shift the layout                                                      | One `Popover` with 44px rows, absolutely positioned     | All three are absolutely positioned · rows are 44px · Escape closes                       | —                                                                             |
| **D7-MF12-T1** | `Checkbox` everywhere   | 11 raw checkboxes                                                                                    | All use the existing `Checkbox`                         | No raw checkbox remains                                                                   | —                                                                             |
| **D7-MF13-T1** | `Switch` everywhere     | Three verbatim-duplicated toggles                                                                    | All use the existing `Switch`                           | No duplicated toggle remains                                                              | —                                                                             |
| **D7-MF14-T1** | Dropdowns become modals | 14 native `<select>` across 9 files, nine of them with no accessible label                           | Every dropdown is a modal form carrying its own heading | Native `<select>` 14 → 0 · every one has an accessible name · closes AX-13                | —                                                                             |
| **D7-MF7-T1**  | Field chrome            | Label, asterisk, hint, error and spacing differ per form                                             | One field chrome                                        | Every field follows it                                                                    | —                                                                             |
| **D7-CS35-T1** | `Skeleton`              | Loading states do not match what they replace                                                        | A skeleton inherits the radius and height of its target | Each skeleton matches its target's shape                                                  | —                                                                             |
| **D7-CS36-T1** | One `Spinner`           | Two spinner mechanisms                                                                               | One                                                     | The second mechanism is gone                                                              | —                                                                             |
| **D7-CS30-T1** | `ProgressRing` reuse    | Tasks header, Initiation accordion and RR group card each draw their own progress                    | All three use the existing `ProgressRing`               | All three use it                                                                          | The RR ring needs **D6-C1-T1** — no ring beside a shut gate                   |
| **D7-CS34-T1** | Honest loading          | Two loading percentages are fabricated                                                               | Both are indeterminate                                  | Neither shows a made-up number                                                            | —                                                                             |

---

## Group 4 · The per-site sweeps

Applying what D3 built. No new decisions — these are mechanical, and the grep suite scores them.

| ID            | Task                        | Before                                                                                                                                                   | After                                                      | Acceptance                                                                                                             | Exit adds                                                                                         |
| ------------- | --------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| **D7-SW-T1**  | Light-theme fills and rules | 12 row separators vanish on light cards so eight list surfaces read as one block; 9 surfaces have no light fill at all, including six Profile stat tiles | Separators and fills resolve in both themes                | Separators that do not flip 12 → 0 · surfaces with no light fill 9 → 0                                                 | —                                                                                                 |
| **D7-SW-T2**  | Page backgrounds and tints  | 42 page-coloured backgrounds across 22 files; 38 surface tints; seven hairline opacities                                                                 | Five tints, one hairline, tokenised backgrounds            | Tints 38 → 5 · hairlines 7 → 1 · no page-coloured background remains                                                   | —                                                                                                 |
| **D7-SW-T3**  | Selected states             | Nine unselected controls read as selected; 13 "selected" treatments                                                                                      | One selected treatment that reads correctly                | Unselected reading as selected 9 → 0 · treatments 13 → 1                                                               | —                                                                                                 |
| **D7-SW-T4**  | Light-mode clay             | `--color-clay` is missing from the light block, so clay surfaces fall back                                                                               | One line adds it; `--color-clay-fg` stays `#9e2d12`        | Every `bg-clay` / `shadow-clay` / `accent-clay` inherits · text contrast stays above 4.5:1                             | Do **not** change `--color-clay-fg` — #ff6b35 is 2.8:1 on light                                   |
| **D7-SW-T5**  | Clay borders off buttons    | Buttons carry a clay border; `outline` and `ghost` differ only by it                                                                                     | The border is gone and the two variants collapse           | The three border sites are cleared · the box does not move · `Input` focus border and spinner rings are **kept**       | —                                                                                                 |
| **D7-SW-T6**  | Touch targets, in order     | Targets are undersized and gaps are tight                                                                                                                | Gaps widen first, then targets grow                        | Gaps land before targets · no control steals its neighbour's taps                                                      | **Order is load-bearing** (BT-17 before BT-9/BT-10)                                               |
| **D7-SW-T7**  | Radius, elevation, motion   | A ragged radius ladder, four `!important` overrides, bare transitions, five dead hover borders                                                           | One ladder, concentric corners, no overrides               | Overrides 4 → 0 · dead hover borders 5 → 0 · transitions are specified                                                 | —                                                                                                 |
| **D7-SW-T8**  | Typography floor            | **161 sites below the 12px floor**; 12 label treatments; heading sizes proliferate                                                                       | Nothing below 12px, one label treatment, two heading sizes | Sub-12px 161 → 0 · labels 12 → 1 · two heading sizes only                                                              | Do **not** let the sweep touch the 16px control size — it re-opens iOS zoom-on-focus on 58 fields |
| **D7-SW-T9**  | Colour literals             | The court map holds 60 hex literals; eight raw-hex badges; chart colours are ad hoc                                                                      | Every colour comes from a token                            | No hex literal remains in the swept files · chart colours are tokenised                                                | —                                                                                                 |
| **D7-SW-T10** | Keyboard and labels         | The member picker, four court comboboxes and three password toggles have no keyboard path; nine selects and three X buttons are unlabelled               | Every control is reachable and named                       | Each named control is keyboard-reachable · every select and X button has an accessible name · Escape closes map popups | —                                                                                                 |
| **D7-SW-T11** | Consent before analytics    | Analytics fires before any consent is given                                                                                                              | A consent banner precedes analytics                        | No analytics call fires before consent · the choice is remembered                                                      | —                                                                                                 |

---

## Group 5 · Labels, copy and the rest

| ID             | Task                       | Before                                                                                                            | After                                                                                                                         | Acceptance                                                                                                                                                                                                                   | Exit adds                                                                                                                                                                           |
| -------------- | -------------------------- | ----------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D7-CS24-T1** | One vocabulary on the card | A member sees "Completed", "Done" and "Score recorded" for the same fixture                                       | A match card shows **Pending** or **Done** and nothing else                                                                   | The four files render only the two words · `Scheduled` and `No show` appear nowhere                                                                                                                                          | —                                                                                                                                                                                   |
| **D7-CON-T1**  | Consent line               | A shared draw carries participant contacts and nobody is told                                                     | Signup and event join each say so in one plain sentence                                                                       | Both surfaces carry the line · the wording matches · nothing else on either screen changes                                                                                                                                   | —                                                                                                                                                                                   |
| **D7-DPC-T1**  | Doubles pool card          | No doubles pool card exists                                                                                       | One card carrying exactly five stats                                                                                          | The five render · it reuses the shared card · no sixth stat appears                                                                                                                                                          | Needs **D7-CS7-T1**                                                                                                                                                                 |
| **D7-STA-T1**  | The stats a member sees    | Surfaces show different stat sets, the streak is derived twice, and upcoming matches are presented inconsistently | Leaderboard, RR table and the **upcoming matches stat cards** all show exactly the ruled stats, and the streak rides the card | The leaderboard row shows matches won, P/G won %, rank move and streak · the RR table shows its four · the upcoming matches stat cards show the member pending fixtures and nothing else · no surface adds a stat of its own | Needs **D7-CS7-T1**, so there is no helper to extract                                                                                                                               |
| **D7-DDW-T1**  | Download Draw              | The only "Download the draw" is an error-boundary fallback                                                        | A real organizer control gives the draw **and** participant contacts in one file                                              | The organizer downloads draw plus contacts · a non-organizer cannot · the fallback is untouched                                                                                                                              | Beta ships this simple version. The **View Draw** remodel, several versions of the same draw each with its own download, is a during or post beta change, not a reason to hold this |
| **D7-LAB-T1**  | One label per stat         | Labels drift per surface                                                                                          | `P/G Won %`, `Wins`, `Matches`, `Group Pts` and the rest read the same everywhere                                             | Each stat has one label everywhere · rank move renders once per row                                                                                                                                                          | —                                                                                                                                                                                   |
| **D7-INI-T1**  | The last `initialOf`       | `ServicesElements.tsx:595` keeps its own copy, out of reach of the profile consolidation                          | It points at the shared one                                                                                                   | No second implementation remains                                                                                                                                                                                             | Needs **D7-CS17-T1**                                                                                                                                                                |
| **D7-CPY-T1**  | Remaining copy defects     | Counts, totals, links and the court-map, services, marketplace and notification copy carry known defects          | Each reads correctly                                                                                                          | Every listed row is corrected · one rewards-available helper remains                                                                                                                                                         | —                                                                                                                                                                                   |

---

## Group 6 · One scoring rule

| ID           | Task                | Before                                                                                                   | After                                                                                     | Acceptance                                                                                                                  | Exit adds                                                                      |
| ------------ | ------------------- | -------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| **D7-G6-T1** | Store what was paid | Two payout tables straddle the server/browser boundary, and the group table adds a bonus nobody received | The server records `points_winner` / `points_loser` on the match and the table reads them | `matchAward` is gone from `src/` · the group table reads stored figures · a rescore updates them · one payout table remains | **Do last** — it touches the scoring path; keep it away from the visual sweeps |

---

## Group 7 · Copy, readability and the chart

| ID           | Task                  | Before                                                                                      | After                                                                                             | Acceptance                                                                                                                                                                                                                                                                                                                                                                                | Exit adds                                                                                             |
| ------------ | --------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- |
| **D7-G7-T1** | Copy sweep            | Strings read as machine-written — em dashes, "simply", three-part lists, "please note that" | Every user-facing string reads like a person wrote it                                             | No em dash stands in for punctuation · the banned words are gone · buttons, empty states, errors and notifications all swept                                                                                                                                                                                                                                                              | **After groups 1–3** — rewriting strings inside components about to be replaced is wasted work        |
| **D7-G7-T2** | Rows fit 5.8 inches   | Rows are checked on desktop previews, and long names truncate to something unreadable       | Every row type is readable on one line at 360px, and a shortened name still identifies the person | A long name keeps the **whole first name** and adds the surname initial, so "Annas Tariq" renders "Annas T" and never "Ann..." · a first name too long for the space truncates at 6 or 7 characters with no surname initial · names never wrap · at most two numbers beside the name · the action slot never shrinks the name below about 40% of the row · contact controls are icon only | Applies to `PersonRow` densities, `ListRow`, `PersonPairRow`, `PersonOption` and the RR standings row |
| **D7-G7-T3** | The leaderboard chart | The current chart plots whole history with axis labels                                      | Last 5 matches, P/G won % and rank, rank axis inverted, no axis labels, annotated points          | Five points maximum · rank 1 sits at the top · annotations read "48% P/G won" and "rank #23" · at 360px only first and last are labelled · one match shows a point and no line · no matches shows an empty state with no axes                                                                                                                                                             | —                                                                                                     |

---

## Sprint exit gate

Beyond every task closing, the sprint closes when the grep suite reports: sub-12px **161 → 0** ·
separators **12 → 0** · light-theme fills **9 → 0** · unselected-as-selected **9 → 0** ·
`confirm()` **5 → 0** · hand-rolled banners **13 → 0** · `matchAward` absent from `src/` ·
design-sync diff clean in **both** cells for all 22 components · and the owner walks every
screen in both themes on a real phone.
