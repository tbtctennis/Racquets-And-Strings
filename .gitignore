node_modules/
dist/
coverage/
/development/
anuj-development/
.firebase/

.env*
!.env.example
serviceAccount.json
functions/.runtimeconfig.json
functions/.secret.local
.secret.local
serviceAccount*.json
*-firebase-adminsdk-*.json
google-services.json
GoogleService-Info.plist
firebase-applet-config.json
*.pem
*.p12
*.p8
*.key

# Local CSO / gstack security reports (not the vendored skill tree)
.gstack/

.DS_Store
*.log

.vscode/
.idea/
*.local
firebase.local.json
firebase-debug.log
playwright-report/
test-results/

# Local data-analysis tooling & Firestore dumps (contains user PII — never commit)
analysis/

# Git worktrees created by Claude Code sessions (local only)
.claude/worktrees/

# Generated handover/audit reports and their spreadsheet exports. Kept on disk locally,
# deliberately out of the repo — they are session output, not part of the app. Listed by
# name rather than as *.md/*.xlsx so CLAUDE.md and the public/ + data/ CSVs stay tracked.
Code_Conflicts_Audit.xlsx
DATA_MIGRATION_REPORT.md
FILE_MAP.md
Function_Inventory.md
Function_Inventory.xlsx
PRODUCT_JOURNEY.md
PROVIDER_GUIDE.md
SECURITY_AND_MOBILE_READINESS.md
Session_Changes_2026-08-12.xlsx
Setup_Documentation.docx
TECHNICAL_HANDOVER.md
Technical_Handover_Anuj_Raja.xlsx
Test_Cases_Gaps.xlsx
Tournament_Logic_Report.md
WORKFLOWS.md
change_reports.xlsx
public/2025League.xlsx

# Stray directory created by a mistyped path (local only)
~/

# Sample dataset generated from the live snapshot by scripts/build-sample-dataset.mjs.
# Derived data, rebuildable in one command — and with --real-names it is real member data.
tests/fixtures/dataset/

# Generated by scripts/start-emulators.mjs when a configured emulator port is busy. Must sit at
# the repo root so relative paths inside it (storage.rules, firestore.rules) still resolve.
firebase.resolved-ports.json
